# TIL × RMK 0.9 系 OLSK60 — 接続経路の点検と、詳細スクロール／エンコーダの表現可否

- Status: Reviewed（ソース照合済み。実機は未確認の項目を `要確認` で明記）
- 作成: 2026-09-06
- 照合対象:
  - RMK: fork `techmech-keeb/rmk` の固定 rev `8bdb0ab8`（`rmk-config` `keyboards/olsk60/Cargo.toml` が参照。
    上流 `rmk-rs/rmk` タグ `rmk-v0.9.0` = `986017d9` の子孫で、上流 main `568770f7`（0.9.0 + 12 コミット）に
    fork パッチ 14 コミットを載せたもの。crate 版は `rmk 0.9.0`）
  - OLSK60 FW: `rmk-config` main `f191d07`（`3.0.0-rc.1`・2026-09-06）
  - TIL: 本リポジトリ main `c7e6dcc`（v1.x 系。`ui/vial.js` / `ui/app.js` / `ui/layouts/olsk60.js`）
- 関連: `docs/roadmap.md`「残課題」、README「RMK 移行時の再確認事項」、
  knowledge-base `02_notes/_open-verifications.md` #27、rmk-config `WATCHLIST.md` T-10

## 1. 結論

1. **Vial 接続経路（Raw HID）は RMK 0.9 系でもプロトコル面の変更なしで通る。**
   TIL が使う 9 コマンドすべてを fork の実ソースで照合し、応答形式・番号体系・ロックの粒度が
   vial-qmk と一致することを確認した（§2）。マトリクステスターのバイト順は 2026-09-02 に
   実機（Vial-RMK）で確認済み（knowledge-base の Entropy 起票記録）。
2. **ただし TIL 側の OLSK60 プロファイルが QMK 版のまま**で、RMK 版に対して 3 点が古い
   （rmk-config `WATCHLIST.md` T-10 と同じ）: Vial UID、matrix 列数（14→13）、
   customKeycodes（名前も個数も違う）。UID は USB VID/PID のフォールバックで救われるので
   表示は切り替わるが、**customKeycodes の不一致でガイドツアーの対象キーが解決できず、
   RMK 版では L2 の設定キーが「USER00」表示になる**（§3）。
3. **詳細スクロール（hi-res / Resolution Multiplier）は「表現できるが、現状は表現していない」。**
   ブラウザの `wheel` イベントはサブノッチ単位で届くので検出は可能。現状はイベント数を
   そのまま数えるため、hi-res 時はライブフィードの ×N が 12 倍に膨れ、標準スクロールとの
   違いが「ノッチが細かい」ではなく「回数が多い」に見える（§4）。
4. **ロータリーエンコーダは現状まったく表現していない。** 配列にエンコーダが無く、回転は
   L0 で `AudioVolumeUp/Down`、L1 で `PageUp/Down` のキー入力として届くだけ（打鍵数に混ざる）。
   L2 の `OCT_UP/DN`（User10/11）は HID 出力が無く、マトリクステスターの対象でもないため
   検出不能。Vial の `GetEncoder`（`0xFE 0x03`）でエンコーダ割当を読み、割当先のキーコードから
   回転を逆引きすれば **L0/L1 の回転と押し込みは表現できる**（§5）。
5. FW 側で 1 件、設計と実装の食い違いを見つけた: **マウスキーのホイール（L3 の `Wh↑↓←→`）が
   Resolution Multiplier を通らない**。hi-res を選んだホストでは 1 tick が 1/12 ノッチになる
   （設計書は「全 producer を L5 に集約し 1 detent を保つ」と規定）。TIL で L3 ホイールキーを
   試すとその差が見える。詳細と扱いは §6（rmk-config `WATCHLIST.md` へ登録）。

## 2. Vial 経路の照合表（TIL 実装 vs RMK fork 実ソース）

TIL は `ui/vial.js` で次のコマンドを使う。RMK 側は `rmk/src/host/via/mod.rs` /
`rmk/src/host/via/vial.rs` / `rmk-types/src/protocol/vial.rs` / `rmk/src/host/via/keycode_convert.rs`
（いずれも `8bdb0ab8`）。

| # | TIL が送るもの | TIL の前提（vial-qmk 実測） | RMK 0.9 fork の実装 | 判定 |
|---|---|---|---|---|
| 1 | `0x01` protocol version | 応答 [1..2] を VIA 版として読む | `VIA_PROTOCOL_VERSION = 0x0009` | ✅ |
| 2 | `0xFE 0x00` keyboard id | [0..3] = Vial protocol（≤0x40 で Vial 機とみなす）, [4..11] = UID | `VIAL_PROTOCOL_VERSION = 6` を LE u32 で [0..4]、UID を [4..12]（`vial.rs` L25-26） | ✅ 形式一致。**❌ UID の値が違う**（§3） |
| 3 | `0xFE 0x01/0x02` vial.json | XZ 圧縮を C# ホストで展開 | `build.rs` が `vial.json` を XZ（level 6）で埋め込み | ✅ |
| 4 | `0x11` layer count / `0x12` get_buffer | **ロック中でも読める**（vial-qmk と同じか要確認だった） | `DynamicKeymapGetLayerCount` / `GetBuffer` に**ロック判定なし**（`mod.rs` L194-211）。BE u16 で書く | ✅ ソース確認（実機 `要確認`） |
| 5 | キーコード番号体系 | Vial protocol ≥6 = modern QMK（`ui/keycodes.js` v6 表） | HID 0x00-0xFF（マウスキー 0xCD-0xDF、`AudioVolUp` 0xA9）、mods 0x0100、MT 0x2000、LT 0x4000、LM 0x5000、TO/MO/DF/TG/OSL/PDF 0x52xx、TD 0x5700、macro 0x7700、Boot 0x7C00、**User(n) = 0x7E00 \| n** | ✅ TIL の decode と一致（`0x7E00..` → customKeycodes 添字） |
| 6 | `0x02 0x03` matrix state | unlock 必須。行ごとに `ceil(cols/8)` バイト **big-endian** | `host_lock` は default feature。`locker.is_unlocked()` のときだけ応答、`rows*ceil(cols/8)` バイトで行内を反転して big-endian（`mod.rs` L66-77）。ロック中は arm を通らず要求バッファをそのまま返す（`run_session` で `input_data = buf`） | ✅ 2026-09-02 に実機で確認済み（KB Entropy 記録） |
| 7 | `0xFE 0x05` unlock status | [0]=unlocked [1]=in progress、以降 unlock keys | 同じ。`unlock_keys = [[0,0],[2,11]]`（Esc + Enter） | ✅ |
| 8 | `0xFE 0x06/0x07` unlock start / poll | poll が [0..2] を返す | 同じ（[2] = カウンタ） | ✅ |
| 9 | （未使用）`0x14` VIA get encoder | — | **未対応**（`warn!("not supported")`） | — 使わないこと |
| 10 | （未使用）`0xFE 0x03` Vial get encoder | — | 対応。要求 [2]=layer [3]=index、応答 **[0..2] = 反時計回り, [2..4] = 時計回り**（BE u16） | §5 で使う |

## 3. TIL 側の未更新 3 点（T-10）と修正案

| 項目 | TIL 現状（`ui/layouts/olsk60.js`） | RMK 版の正本 | 影響 | 修正案 |
|---|---|---|---|---|
| Vial UID | `C4 37 B8 91 73 93 22 AD`（vial-qmk 版） | `1E EB CB 50 9F 6B 94 EE`（`rmk-config` `keyboards/olsk60/build.rs` L82） | UID 不一致 → `boards.js` が VID/PID `0x746D:0x0102` で一致させるので**表示は切り替わる**。ただし将来 VID/PID を共有する別ボードが出ると誤判別 | `match.uid` を配列にして両 UID を登録（QMK 版 2.0.x 利用者が残るため置換ではなく追加） |
| matrix 次元 | rows 6 / cols **14**（WebHID 単体時の fallback） | rows 6 / cols **13**（`vial.json`・`keyboard.toml`） | `ceil(14/8) = ceil(13/8) = 2` バイトで同じため**実害なし**。キオスク経由では端末の vial.json を優先するので影響なし | 13 に直す（QMK 版は 14 だが、`bytesPerRow` が同じなので QMK 版でも動く） |
| customKeycodes | 20 個（`Precision`, `Balanced`, `Fast`, `CustPrec`, `CustFast`, `Spd+`…, `AL 150ms`, `Scroll`） | 23 個（`TP_SPD_1..5`, `AL_150MS/400MS/800MS`, `SND_TOG`, `SND_MODE`, `OCT_UP/DN`, `BASE_UP/DN`, `ACC_UP/DN`, `DEC_UP/DN`, `AML_TOG`, `TP_SCROLL`, `TP_SCROLL_L1..L3`。shortName は `TP\nSpd1` 等） | ①WebHID 単体時は fallback 名が全部ずれる ②キオスク経由でも `shortName`（改行入り）で表示 ③**ガイドツアー**（`olsk60.tours.js`）は `custom: "Precision"` 等の**名前で解決**するため RMK 版では対象が見つからずステップが成立しない | ①fallback を RMK の `name` 基準へ更新 ②`shortName` の `\n` を空白に正規化 ③ツアーの `target.custom` を RMK 名へ書き換え、LED 点滅の文言（青 2 回 等）を RMK 版の実装（`ws2812_status.rs`）で再確認して同期。QMK 版とは別プロファイル（`olsk60v2-rmk`）に分ける案も可 |

補足: ボトム行の matrix 位置は vial.json の 3-split 相当（`[4,2] [4,4] [4,5] [4,7]`）で、
`keyboard.toml` の row 4 と突き合わせると `[4,3]/[4,4]` に `MouseBtn1/2`、`[4,6]` に `Space` が
割り当てられている。TIL の Space は `[4,4]` なので、**マトリクス点灯で Space を押したとき
「Mouse2」刻印の位置が光る可能性がある**。QMK 版の実機で確認済みの配置と同じ値を RMK 版が
移植しているため矛盾しないはずだが、本書では断定できない。`要確認`（RMK 版実機で Space と
TP ボタンの点灯位置を見る）。

## 4. 詳細スクロール（hi-res）の表現可否

### 4.1 FW → OS の事実（fork 実ソース）

- 複合インターフェースのマウスレポート（report id `0x02`）は `wheel: i8` / `pan: i8` を持ち、
  Wheel と AC Pan それぞれの LOGICAL collection に **Resolution Multiplier feature**
  （usage 0x48、report id `0x05`、logical 0..1 / physical 1..**12**）を宣言する（`rmk/src/hid.rs` L176-232）。
- ホストが `SET_FEATURE` で logical 1 を書くと `rmk::usb::resolution_multipliers()` が
  `(12, 12)` を返し、TrackPoint スクロール（`tp_device.rs` `emit_report`）は 1 ノッチを 12 count に
  分けて送る。書かれなければ 1 = 1 ノッチ（detent）。USB 切断・リセットで 1 に戻る。
- Windows（8.1 以降）と Linux（5.0 以降）は feature を読んで hi-res を選ぶ。macOS は保留
  （rmk-config `tp_hires_tenkey_delivery_plan.md`「現在地」）。展示機は Windows なので hi-res 前提。
- **BLE には feature が無い**（USB のみ）。Platyx 無線時は detent 固定になる。

### 4.2 OS → TIL（Chromium / WebView2 の `wheel` イベント）

Windows は hi-res 時、1 count ごとに `WHEEL_DELTA/12 = 10` の `WM_MOUSEWHEEL` を上げる。
Chromium はこれを **ノッチの分数**（`wheel_ticks = 10/120`）として扱い、`wheel` イベントに
次の形で出す（Chromium の実装から。WebView2 実機で `要確認`）:

| プロパティ | 標準スクロール（1 ノッチ） | hi-res（1 count = 1/12 ノッチ） |
|---|---|---|
| `deltaMode` | 0（pixel） | 0 |
| `deltaY` | ±100 前後（OS のスクロール行数設定に依存） | ±8.3 前後（= 1/12） |
| `wheelDeltaY`（非標準・Chromium） | ±120 | ±10 |
| イベント数 / ノッチ | 1 | 12（TrackPoint の押し込み量次第で 1 パケットに複数 count） |

したがって **`|wheelDeltaY| % 120 !== 0`（または `|deltaY|` がノッチ相当より明らかに小さい）
なら hi-res が交渉されている**と判定できる。精密タッチパッドも同じ形で届くので、
「hi-res 判定」は「サブノッチ入力を受け取っている」の意味で表示する。

### 4.3 TIL の現状（`ui/app.js` `wheel` ハンドラ）

- 合計スクロール量 = Σ(|deltaY| + |deltaX|)（px 換算）。hi-res でも 1 ノッチあたりの合計は同じなので**この数字は壊れない**。
- 方向表示・シェブロン（1 イベント 3 本）・ライブフィード（同種連続は ×N 集約）は**イベント単位**。
  hi-res では 1 ノッチで 12 イベント → フィードの ×N が 12 倍、シェブロンが 36 本/ノッチ（上限 60 で頭打ち）。
  「細かい」ではなく「多い」に見える。
- 横スクロール（AC Pan = TrackPoint の横スクロール・L3 `Wh←→`）は合計量に足すだけで、方向表示が無い。

### 4.4 表現案（実装は別 PR）

1. **解像度インジケータ**: INPUT MONITOR のスクロール欄に「標準 / 高解像度（1/12）」を表示。
   判定はサブノッチ入力を初めて受けたら「高解像度」、以降ノッチ整数の入力だけが一定時間続いたら「標準」へ戻す。
2. **ノッチ換算カウンタ**: Σ|wheelDeltaY|/120 を「ノッチ換算」として併記（px 合計は残す）。
   hi-res でも標準でも同じ 1 ノッチ = 1。
3. **フィードの集約単位をノッチにする**: サブノッチ入力は 1 ノッチ分たまるまで 1 行にまとめ、
   「スクロール ↓ ×3（高解像度）」のように出す。
4. **シェブロンの本数をイベント数でなく量に比例させる**: 1 イベントあたり `3 × |wheelDeltaY|/120` 本
   （最低 1 本）。hi-res では連続した細かいシェブロンが流れ、滑らかさがそのまま見える。
5. **横スクロールの方向表示**（◀ ▶）を追加。TrackPoint スクロールと L3 `Wh←→` の両方で使う。
6. `deltaMode` が 1（line）/2（page）の環境（Firefox 等）は px 換算せずノッチ換算だけ表示する。

実機で確認すること（`要確認`）: Surface Pro 7 + WebView2 で表 4.2 の値が出るか／
TrackPoint を軽く押した最小入力で 1 count が 1 イベントとして届くか／
Windows の「一度にスクロールする行数」を変えたとき `deltaY` だけが変わり `wheelDeltaY` は変わらないか。

## 5. ロータリーエンコーダの表現可否

### 5.1 FW の事実

- `keyboard.toml` `[[input_device.encoder]]`: GP15/GP14、`phase = "resolution"`, `resolution = 2`
  （1 ノッチ = 1 アクション。実機校正済み）。回転は `KeyboardEvent::rotary_encoder(id, direction, pressed)`
  として **押下 → 離し の 2 イベント**で流れ、レイヤーごとの encoder map でキーに変換される。
- 割当（`keyboard.toml` `encoders`）:

  | レイヤー | 時計回り | 反時計回り | ホストに届く形 |
  |---|---|---|---|
  | L0 base | `AudioVolUp` | `AudioVolDown` | Consumer report → Windows が `VK_VOLUME_UP/DOWN` に変換 → `KeyboardEvent.code = "AudioVolumeUp"/"AudioVolumeDown"`（`要確認`: WebView2 で keydown が発火するか） |
  | L1 fn | `PageUp` | `PageDown` | `code = "PageUp"/"PageDown"` |
  | L2 control | `User10`（OCT_UP） | `User11`（OCT_DN） | **HID 出力なし**（FW 内部の音階設定） |
  | L3 mouse | `_` | `_` | 透過 → L0 の音量 |

- **押し込み**（GP13）は matrix に無い直接入力で、`encoder_switch.rs` が合成位置 **`(5,12)`** に注入する。
  `keyboard.toml` の `(5,12)` は全レイヤー `Down`（＝ `ArrowDown`）。マトリクステスターの
  応答にも `(5,12)` が立つ（`read_matrix_state` は注入後の状態を見るため。`要確認`）。
- Vial 側: `vial.json` はエンコーダを `0,0 e` / `0,1 e` として持ち、layout option 1「RotaryEncoder」で
  `(5,12)` のキーを出す。割当の読み出しは `0xFE 0x03`（§2 #10）。

### 5.2 TIL の現状

- `ui/layouts/olsk60.js` に**エンコーダの描画要素が無い**。`(5,12)` のキーも無い。
- 回転は `keydown` としてしか届かず、`registerKeyDown` が**打鍵数に加算**して OSD に
  `AudioVolumeUp` の生の `code` を出す（`OSD_LABEL` に無い）。配列に無いので光らない。
- 押し込みは `ArrowDown` として届き、**矢印キーの ↓ が光る**（本物の ↓ と区別できない）。

### 5.3 表現案（実装は別 PR）

1. **プロファイルにエンコーダを追加**: `encoder: { x, y, index: 0, switchMatrix: [5, 12] }`
   （位置は KLE / 実機で `要確認`。vial.json の KLE ではボトム行の後ろに置かれている）。
   描画はノブ + 回転方向のインジケータ（↻ / ↺）+ 押し込みで点灯。
2. **回転の検出は Vial の encoder map から逆引き**: 接続時に各レイヤーの `0xFE 0x03` を読み、
   `{ layer → { cw: keycode, ccw: keycode } }` を持つ。`keydown` の `code` が現在レイヤーの
   cw/ccw に対応するキーコード（`AudioVolumeUp` ↔ 0xA9、`PageUp` ↔ 0x4B…）と一致し、かつ
   その物理キーがマトリクス上で押されていなければ「エンコーダ回転」と分類する。
   リマップ耐性はツアーと同じ考え方（キーコード指定を実行時に解決）。
   - Vial 未接続・ロック中は L0 の既定（音量）だけをフォールバックで見る。
   - L2 の `OCT_UP/DN` は HID 出力が無いので**検出できない**（表示上は「このレイヤーの回転は
     画面に出ません」と注記するか、ツアーで L2 の回転を扱わない）。
3. **打鍵数から除外し、INPUT MONITOR に「エンコーダ ↻ ×N」として集約表示**。
   OSD は `Vol+` / `Vol-` / `PgUp` の刻印で出す。
4. **押し込み**: `(5,12)` のマトリクス状態、または `ArrowDown` keydown の時点で `(4,11)` が
   押されていなければエンコーダ押下とみなす。Vial unlock 済みなら前者を優先。
5. **ガイドツアー**: 「ノブを右に 3 回」「押し込んで…」のステップを追加できる
   （検知フックは 3 関数に集約されているので、encoder 分類を 1 フック足す）。

実機で確認すること（`要確認`）: WebView2 で `AudioVolumeUp` の keydown が届くか
（届かない場合は C# 側の低レベルフックで `VK_VOLUME_UP/DOWN` を拾って `type: "key"` で
転送する。キオスクモード限定）／`(5,12)` がマトリクステスターに現れるか／
1 ノッチで keydown が 1 回（`repeat` なし）か。

## 6. FW 点検の結果（rmk-config main `f191d07` / 3.0.0-rc.1）

### 6.1 ホスト側で実施できた検証

| 検証 | 結果 |
|---|---|
| `scripts/verify-local.sh --fast`（fmt → ホスト単体テスト → docs 検査） | **通過**。`olsk60-logic` 45 tests passed / 0 failed。docs メタデータ・参照・上流レジストリの警告 0 件 |
| `scripts/build-firmware.sh olsk60 vial`（`cargo build --release`、`thumbv6m-none-eabi`） | **通過**。`.uf2` 生成・`check-uf2.sh` 合格（本書末尾「ビルド結果」） |
| 実機（OLSK60 / 展示 PC） | **未実施**（本セッションに実機なし）。CHANGELOG によれば 2026-09-06 に両 variant で実機確認済み |

### 6.2 見つかった食い違い: マウスキーのホイールが Resolution Multiplier を通らない

- 事実: `rmk::usb::resolution_multipliers()` の呼び出し元は olsk60 の `tp_device.rs` だけ
  （fork 内の `rmk/src` に他の呼び出しなし）。RMK 本体のマウスキー（`rmk/src/keyboard/mouse.rs`）は
  `MouseWheelUp/Down/Left/Right` を `wheel = ±1` の生値で送る。
- 設計上の要求: `tp_hires_tenkey_delivery_plan.md`「TrackPoint と Hi-Res の層境界」は
  「mouse keys・joystick・他 pointing device が従来の `wheel=1` を直接送る構造は禁止。同じ field を
  使う全 producer を L5 へ集約し、1 detent を送る producer はホスト倍率 N なら N counts へ」と規定。
  `tp_driver_remake_design.md` も「host 倍率適用後も既存 mouse keys の 1 detent を 1 detent のまま維持」。
- 帰結（ソースからの推定・実機 `要確認`）: hi-res を選んだ Windows で L3 の `Wh↑↓←→` を押すと、
  1 tick が 1/12 ノッチになり、標準ホストの 12 分の 1 しか進まない（リピートで補われる分を除く）。
  TrackPoint スクロールは正常。
- 扱い: rmk-config `WATCHLIST.md` に W 行として登録（本書と同じ枝で更新）。修正は fork 側で
  `MouseReport` 送出時に wheel/pan へ倍率を掛けるのが最小案。TIL で L3 ホイールキーを
  デモに使う場合はこの差を承知しておく。

### 6.3 その他の確認事項

- fork 固定 rev は上流 `rmk-v0.9.0`（`986017d9`）を含む（`git merge-base --is-ancestor` で確認）。
  `WATCHLIST.md` の「タグ = `986017d9`」の記載と一致。
- `host_lock` は `rmk` の default feature で、`Cargo.toml` の `features = ["rp2040"]` は
  default を外していない → マトリクステスターはロック解除後に応答する（§2 #6）。
- Vial の `unlock_keys` は Esc + Enter で TIL の unlock ウィザードの説明どおり。

## 7. 推奨する次の作業（優先順・別 PR）

| 順 | 作業 | リポジトリ | 規模 |
|---|---|---|---|
| 1 | T-10: UID 追加・cols 13・customKeycodes を RMK 名へ・`shortName` 正規化・ツアー対象名の更新 | TIL | 小 |
| 2 | ツアー文言（LED 点滅・速度プロファイル）を RMK 版の実装で再確認して同期 | TIL（rmk-config を参照） | 小〜中 |
| 3 | hi-res 表現（§4.4 の 1〜5） | TIL | 中 |
| 4 | エンコーダ表現（§5.3 の 1〜4） | TIL | 中 |
| 5 | マウスキーのホイールを倍率経路へ（§6.2） | rmk fork / rmk-config | 小（実機確認 1 回） |
| 6 | §3 補足のボトム行 matrix 位置、§4.2 の値、§5 の keydown 到達を実機で確認 | 実機 | — |

## ビルド結果（2026-09-06・本セッションのコンテナ）

`bash scripts/build-firmware.sh olsk60 vial`（rmk-config main `f191d07`、`cargo build --release`
target `thumbv6m-none-eabi`、`cargo-binutils` + `cargo-hex-to-uf2` を追加導入して実行）:

```text
Finished `release` profile [optimized + debuginfo] target(s)
✅ olsk60_rmk_v3.0.0-rc.1_vial_20260906-f191d07.uf2: 665 ブロック / family 0xe48bff56 (rp2040) / 0x10000000-0x10029800
EXIT=0
```

`.uf2` が生成され `scripts/check-uf2.sh` を通過した。**書き込み・実機動作は未実施**。
